# Top-level .gitignore for the repository

# Ignore local environment files (exact names). Do NOT use patterns like `.env*` here
# because we want to allow committing env examples such as `.env_example` or `.env.example`.
.env
server/.env
client/.env

# Node modules and log files
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Generic logs folder
logs

# Dist/build folders
dist/
build/
dist-ssr
*.local

# Editor directories and files (from client/.gitignore)
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
