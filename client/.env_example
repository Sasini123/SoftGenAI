# Example environment variables for the client (Vite)
# IMPORTANT: Variables exposed to the browser must begin with `VITE_`.
# Do NOT put any private secrets here. If you need a secret (API key), keep it on the server
# and call the server from the client.

# Base URL for API requests (point to your server or a deployed API gateway)
VITE_API_BASE_URL=http://localhost:3000

# Public-ish keys that are safe to expose (replace with placeholders)
VITE_MAP_SERVICE_KEY=your_map_service_key_here

# If you must test a third-party key in the browser, put a placeholder here (not recommended)
VITE_OPENAI_CLIENT_KEY=your_openai_client_key_here

# App mode
VITE_APP_ENV=development
