// Placeholder file retained to avoid breaking relative imports referenced in docs.
// Google OAuth was removed in favor of first-party email/password auth.

export default function GoogleLoginButton() {
  return null;
}
