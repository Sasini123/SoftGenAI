# Example environment variables for server
# Copy this file to `.env` and fill in real secrets on your machine or in your deployment
# Do NOT commit your real `.env` file — it is ignored by `.gitignore`.

# Server port
PORT=5000

# Database connection (example formats)
# Postgres: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@host:5432/dbname

# JWT secret for signing tokens
JWT_SECRET=your_jwt_secret_here

# API keys
OPENAI_API_KEY=your_openai_api_key_here

# Node environment
NODE_ENV=development
